import {
  Slices,
  convertSelector,
  convertActionSelector,
  getSlice,
  setSlice,
  updateStateWithSlice,
} from "./slices";
import type { Selector } from "./slices";

export type { Selector };
export {
  Slices,
  convertSelector,
  convertActionSelector,
  getSlice,
  setSlice,
  updateStateWithSlice,
};
