{
  "name": "@sandstack/neuron",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test:watch": "vitest",
    "test": "vitest run",
    "build": "tsc && npm run test && vite build --config vite.config.ts"
  }
}
